entity andTest is
end andTest;
--
-- function setup_test
--    * Hier wird die Testumgebung aufgesetzt              
--    * D.h Register werden erstellt
--    * und man sollte eine einfache möglichkeit haben, Inhalte der Register zu ändern


architecture T of andTest is
	component MaxCPU
	generic (
		-- Rufe setup_test auf
		-- Teste nun And mit allen möglichen Kombinationen von Registeradressen
		-- assert (x = y and z) für verschiedene Beispielwerte
		-- assert (old_register unverändert) falls old_register nicht an der Operation beteiligt war
	);

	end component;

	begin

end T;
